import React from 'react';
import {Link} from "react-router-dom";

import * as ROUTES from '../../constants/routes';

const NotesButton = () => (
    <button class="button-circle blue" onClick={
        <form>
            <label>
                Name:
                <input type="text" notes="notes" />
            </label>
        </form>
    }> +
    </button>
)

export default NotesButton;

